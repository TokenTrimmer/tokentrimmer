# Third-Party Licenses

TokenTrimmer (`tt`) is licensed under Apache-2.0 (see LICENSE). The distributed
binary statically links the third-party Rust crates listed below. This file is
generated by `cargo about generate` — do not edit by hand; run
`scripts/gen-third-party-licenses.sh` to regenerate.

{{#each overview}}
- {{{name}}} ({{count}})
{{/each}}

{{#each licenses}}
## {{{name}}}

Used by:
{{#each used_by}}
- [{{{crate.name}}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{{text}}}
```

{{/each}}
