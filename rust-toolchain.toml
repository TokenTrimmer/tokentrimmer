[toolchain]
# 1.88 is the floor: `cargo-platform 0.3.2` (transitive through cargo-chef)
# requires it. Bump in lockstep with CI and the Dockerfile chef stage.
channel = "1.88.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"
