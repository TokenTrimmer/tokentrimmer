# A semantic version is stable across calls — it is not a timestamp/uuid and
# must not be flagged as a cache-buster.
system_prompt = "You are assistant v1.2.3. Follow the rules and keep answers short."
