# A short hex color in a static system prompt. Not a long random token, so it
# must not trip the random-token detector.
system_prompt = "You are a UI assistant. The brand color is #ff8800. Keep responses brief."
