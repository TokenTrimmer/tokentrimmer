# CLAUDE.md

## About This Project

A web application built with Next.js and Prisma.

## Development Setup

1. `npm install`
2. `cp .env.example .env` — fill in your values
3. `npm run dev`

Never commit real credentials; use .env (gitignored).
