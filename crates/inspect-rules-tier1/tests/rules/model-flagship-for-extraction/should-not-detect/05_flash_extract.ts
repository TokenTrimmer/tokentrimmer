// Negative: Gemini Flash for extraction — small model, fine.
const systemInstruction = "Extract structured data from text.";
const model = "gemini-2.0-flash";
// Using a small model (flash) for extraction is acceptable.
