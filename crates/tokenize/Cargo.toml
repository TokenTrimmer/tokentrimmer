[package]
name = "tt-tokenize"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared token estimation (tiktoken cl100k + heuristic fallback) used by preview, dispatch, and routing."

[dependencies]
tiktoken-rs.workspace = true

[dev-dependencies]
# Header-only decode of the inline image bytes in the dimension-parser tests.
base64 = "0.22"

[lints]
workspace = true
